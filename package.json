{
  "name": "ai-commit-pr-generator",
  "displayName": "AI Commit & PR Generator",
  "description": "Generate AI-powered git commit messages (Conventional Commits) and PR descriptions using OpenRouter.",
  "publisher": "wsilva",
  "version": "0.1.4",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "git",
    "commit",
    "ai",
    "openrouter",
    "pull request",
    "conventional commits"
  ],
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/w-osilva/ai-commit-pr-generator"
  },
  "extensionDependencies": [
    "vscode.git"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "aiCommitPr.generateCommit",
        "title": "Generate AI Commit Message",
        "icon": "$(sparkle)",
        "category": "AI Commit"
      },
      {
        "command": "aiCommitPr.generatePR",
        "title": "Generate AI PR Description",
        "icon": "$(sparkle)",
        "category": "AI Commit"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "aiCommitPr.generateCommit",
          "group": "navigation",
          "when": "scmProvider == git"
        }
      ],
      "view/title": [
        {
          "command": "aiCommitPr.generatePR",
          "group": "navigation",
          "when": "view == github:createPullRequestWebview"
        }
      ]
    },
    "configuration": {
      "title": "AI Commit & PR Generator",
      "properties": {
        "aiCommitPr.openRouterApiKey": {
          "type": "string",
          "default": "",
          "markdownDescription": "OpenRouter API key. Get one at [openrouter.ai/keys](https://openrouter.ai/keys)"
        },
        "aiCommitPr.model": {
          "type": "string",
          "default": "openai/gpt-oss-120b:free",
          "description": "OpenRouter model ID (e.g. nvidia/nemotron-3-super-120b-a12b:free, google/gemma-4-31b-it:free, anthropic/claude-3.5-sonnet)"
        },
        "aiCommitPr.baseBranch": {
          "type": "string",
          "default": "main",
          "description": "Base branch for PR diff comparison (main, master, develop, etc.)"
        },
        "aiCommitPr.commitPrompt": {
          "type": "string",
          "default": "You are a senior engineer writing a git commit message for a production codebase.\n\nGiven these staged changes:\n\n{diff}\n\nWrite a commit message following Conventional Commits (https://www.conventionalcommits.org).\n\nFormat: <type>(<scope>): <short summary in imperative mood, max 72 chars>\n\n[body — optional, only if the WHY or business context is non-obvious]\n\nRules:\n- Types: feat, fix, refactor, perf, test, chore, docs\n- Scope: the module, page, or domain affected (e.g. guardian-dashboard, billing, auth)\n- Summary: imperative mood (\"add\", \"remove\", \"replace\"), no period at the end\n- Body: explain WHY the change was made and any non-obvious business logic or trade-offs. Skip it if the summary is self-explanatory. Do NOT describe what the diff shows — the reader can see that.\n- No bullet points in the body; use plain prose, max 3 sentences\n- Do not mention file names or line numbers\n\nOutput only the commit message, no explanation.",
          "description": "Custom commit prompt. Use {diff} as placeholder. Leave empty to use the default prompt."
        },
        "aiCommitPr.prPrompt": {
          "type": "string",
          "default": "Generate a pull request description based on the following context.\n\n<git_history>\n{history}\n</git_history>\n\n<pull_request_template>\n{template}\n</pull_request_template>\n\nProvide a title for the pull request and a description following the pull request template above.\n\nThe title must be in Conventional Commits format: <type>(<scope>): <short summary>.\n\nWhen writing the description, follow these guidelines:\n\n- Be extremely clear, direct, and concise (1-2 sentences max for each section). No emojis.\n- Bullet lists are fine, but don't bold the start of each bullet.\n- Focus on \"why\" the changes were made and \"how\" they achieve the goal, rather than describing \"what\" was changed.\n- Avoid unnecessary adjectives or salesy language.\n- Omit any template section entirely if you have nothing meaningful to add.\n- Unless the git history is completely focused on the test suite, do not include information about test changes in the description.\n\nReply with ONLY a valid JSON object: {\"title\": \"...\", \"body\": \"...\"}. No text outside the JSON.",
          "description": "Custom PR prompt. Use {history} (JSON commit history) and {template} as placeholders. Leave empty to use the default prompt."
        }
      }
    }
  },
  "scripts": {
    "compile": "webpack --mode development",
    "build": "webpack --mode production",
    "watch": "webpack --mode development --watch",
    "vscode:prepublish": "npm run build",
    "release": "bash scripts/release.sh",
    "release:minor": "bash scripts/release.sh minor",
    "release:major": "bash scripts/release.sh major"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.1",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.3",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.4"
  }
}
